<div>
    <h2> {{text}} </h2>
      {{#if hint}}
        <p>
          <i> Hint: {{hint}}</i>
        </p>
      {{/if}}
    <a href='{{id}}?side={{flipTo}}')> {{flipToDisplay}} </a>
  <br>
    <a href='/cards'>Another question!</a>
</div>
